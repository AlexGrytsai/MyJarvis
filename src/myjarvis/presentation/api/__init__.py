"""
This package defines the API endpoints for the MyJarvis application.

It is responsible for exposing the application's functionality to the outside
world through a RESTful API. The API is versioned to allow for future changes
without breaking existing clients.

This package contains the following modules:
- `v1`: Contains the first version of the API.
- `dependencies`: Contains dependency injection providers for the API.
"""
